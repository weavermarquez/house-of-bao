[project]
name = "lof-evals"
version = "0.1.0"
description = "Laws of Form arithmetic evaluation suite"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.0",
    "anthropic>=0.39",
    "google-generativeai>=0.8",
]

[tool.uv]
dev-dependencies = []
